要求：采用叙述文写作，不用讲究文采，简单直接、把自己的思路和想法讲清楚即可；不超过3000字。
一、重点工作回顾
请对过去半年的重点工作目标及完成情况进行总结复盘，分析其中的亮点和不足。
1. 重点工作梳理
   重点工作是什么？（结合岗位核心职责与团队阶段目标，筛选3-5项投入最多、与岗位最相关的工作）
   1.1 快照相关工作
- 实现快照链压缩
- 实现删除仓库的回收
- 实现快照计费
- 补充测试
  1.2 FE 内存优化：减少多 tablet 下 FE 的内存占用
  1.3 存算分离fdb/ms减负，FE 不会大量重复地去 ms 拉取数据

2. 重点工作目标与关键动作
   以上重点工作在过去半年的核心目标是什么？采取了哪些关键策略或关键动作？
   2.1 FE 内存优化：减少多 tablet 下 FE 的内存占用
- 重构 Tablet 和 Replica 类
- 优化 TabletInvertedIndex，CloudGlobalTransactionMgr，CloudTabletStatMgr
  2.2 存算分离fdb/ms减负, FE 不会大量重复的去 ms 拉取数据
- 优化 get_version 请求
    - 采用主动通知的机制更新 table 和 partition 的 version，保持内存中缓存的 version 为最新；
    - 增加了缓存 version 的校正机制；
- 优化 get_tablet_stats 请求
    - 采用主动通知的机制更新 tablet stats
- checkpoint 持久化 tablet stats 信息
    - 不写入 edit log，在做 checkpoint 时存储 tablet stats，尽可能的保持 stats 较新
- FE 支持主动和被动限流
    - 主动限流：支持根据 QPS 限流，支持根据请求的代价(kv数)限流
    - 被动限流：根据 MS 的反压，动态调整 QPS 限制

3. 结果达成情况
   亮点写超出目标/获得认可的成果，可以附具体数据；不足写未达预期的情况，说明原因
   3.1 FE 内存优化
- 效果
    - 对存算分离，100万 tablets 大约减少 600MB 的内存 (这个是中期的测试，后面还有一些优化)
    - 对存算一体，也有优化
      3.2 存算分离fdb/ms减负, FE 不会大量重复的去 ms 拉取数据
- 效果
    - 基本消除了查询的 get_version 请求
    - 对数据无变更的 tablet，不会重复的 get tablet_stats；很大程度上缓解了stats 数据滞后导致的查询不准确问题
    - 通过持久化 stats 信息，缓解了 FE 重启后统计信息不准确导致查询不准确的问题
- 不足
    - 测试排期较晚
    - 可能是一个中期的方案？长期的方案让 FE 订阅 MS 的变更？减少 master FE 同步给 follower 的压力；代码解耦，不需要考虑哪些操作会带来更新，只接收更新的通知。
      二、成长与反思
      可以从以下几个方面思考：
      1）新收获了哪些知识、经验或技能，有哪些认知迭代？（可以是专业知识、团队管理技能，或者是对业务的认知迭代）
      2）对于公司新发布的文化价值观，你在哪些方面有提升？（也可以分享印象深刻的关键时刻）
      3）列举上半年遇到的1-2个典型问题及解决过程，总结个人收获
- 多使用 AI 工具，AI 开发的效率很高；跟 AI 交流有时也能获得一些新的思路；需要仔细的 review AI 写的代码。
  三、工作计划
  请对未来半年的工作及个人成长进行规划，展示重要的方向性和策略性思考。
1. 下阶段的工作目标、策略或者思路是什么？
- 存算分离备份恢复相关工作
-  持续解决一些问题
2. 下阶段的个人成长目标是什么？
-  更加高效的使用 AI 工具
3. 针对未来的目标，需要哪些相关的资源支持和帮助？

四、对团队或公司发展有哪些建议？
1. 结合AI时代的机遇与挑战，有哪些建议？
   1）在产品和服务方面，你觉得我们有哪些机会和挑战？
   2）在效率提升方面，你觉得我们如何用好AI工具助力个人效能（效果+效率）提升？基建如何适配AI时代？
   3）举一个 AI 对你研发和问题定位帮助非常大的例子，具体怎么用的
2. 在其他方面，有哪些建议？
   附录
