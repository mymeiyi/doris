# Cloud MemTable 前移测试结论简报

测试日期：2026-09-08～09；整理日期：2026-09-22。
本报告依据测试机保存的请求记录、汇总、部署信息和结果校验文件整理，未重新运行测试，不能作为当前分支的性能验收结果。

## 结论

- 在本次单 tablet、高并发 Stream Load 场景中，**前移 + Sink 上传 + 目标 BE 异步预热**改善了三个 BE 的 CPU 分布；DUP、MOR 的吞吐提升明显，AGG 次之，MOW 提升较小。
- **只做前移、仍由目标 BE 上传文件，不保证吞吐提升**。旧版测试中目标 BE CPU 已下降，但写入等待仍占主要耗时；并发从 48 增到 96 没有改善吞吐。
- CPU 分散不等于每台 BE 的利用率都下降。DUP 开启后其他两个 BE 承担更多工作，总平均占用核数上升，但完成同样请求的总 CPU 核秒从约 4053 降到 2887。
- Group Commit `async_mode` 提高了请求接收速度，但必须另看数据全部可见时间，不能把异步响应吞吐当成提交吞吐。

## 主要对照：9 月 9 日异步预热版本

环境：三个 16 核 BE（`172.20.49.5/.6/.7`）；客户端在 `.6`，目标 tablet 在 `.7`。
FE 版本为 `0779c7c5159`，BE 版本为 `6e2bb1b46db`，来自各目录的 `deployment_before.tsv` / `deployment_after.tsv`。
每轮 48 并发、7500 次正式请求，每次 10000 行；三台 BE 各接收 2500 次请求，另有 30 次预热。
各模型均比较关闭前移与开启前移 + Sink 上传，双方关闭 packed file，普通 Stream Load 使用 `group_commit:off_mode`。
自动 compaction 保持开启。模型之间有不同的聚合、去重语义，只比较同模型的 OFF/ON。

耗时不含预热和后续校验；吞吐为 7500 / 耗时，提升按未四舍五入的数据计算。

| 模型 | 总耗时 OFF → ON（秒） | 吞吐 OFF → ON（请求/秒） | 吞吐提升 | 单次平均耗时 OFF → ON（毫秒） |
|---|---:|---:|---:|---:|
| DUP | 224.53 → 90.54 | 33.40 → 82.84 | +148.0% | 1399.36 → 481.31 |
| AGG | 56.91 → 48.47 | 131.79 → 154.73 | +17.4% | 327.23 → 300.03 |
| MOR | 114.58 → 58.56 | 65.46 → 128.07 | +95.7% | 696.37 → 333.21 |
| MOW | 305.72 → 278.09 | 24.53 → 26.97 | +9.9% | 1949.64 → 1774.37 |

CPU 为请求执行区间内 BE 进程平均占用核数，16 核代表一台 BE 满载；包含该进程的后台工作，客户端进程不计入。

| 模型 | OFF：.5 / .6 / .7 | ON：.5 / .6 / .7 |
|---|---|---|
| DUP | 1.07 / 1.18 / 15.80 | 8.91 / 9.17 / 13.81 |
| AGG | 2.52 / 2.00 / 14.85 | 4.40 / 4.37 / 9.15 |
| MOR | 1.52 / 1.04 / 15.41 | 8.35 / 8.46 / 13.30 |
| MOW | 1.04 / 0.68 / 10.97 | 3.18 / 3.01 / 7.76 |

`.7` 仍承担较多工作，不能称为完全均衡。响应阶段计时显示：

- AGG 的 `WriteDataTimeMs` 从 272.81 降到 159.57，但 `CommitAndPublishTimeMs` 从 44.54 增到 131.04，抵消了部分收益；仅凭这一合并指标不能归因于 FE→MS 提交 RPC。
- MOW 开启后的 `CommitAndPublishTimeMs` 为 1280.16 ms，约占服务端 `LoadTimeMs` 的 72%；提交/发布阶段仍值得优先排查，不能仅根据 CPU 较低继续增加并发。

## DUP + Group Commit async_mode

使用上述 9 月 9 日版本、48 并发、7500 × 10000 行，开启前移与 Sink 上传，关闭 packed file。
`group_commit_interval_ms=60000`，`group_commit_data_bytes=134217728`。

- 全部请求返回：**63.32 秒，118.45 请求/秒**，单次平均 395.54 ms。
- 从首个请求开始到全部数据可见：**123.97 秒，60.50 请求/秒**；请求记录对应 229 个 Group Commit 事务。
- 请求区间 CPU：`.5/.6/.7 = 5.12/4.55/5.14` 核；按全部可见区间计算为 `2.67/2.37/3.23` 核。
- 普通 DUP 前移 + Sink 上传为 90.54 秒：async 接收更快，但本轮全部可见更晚；没有 Group Commit 关闭前移的配对轮，不能单独量化 Group Commit 场景中的前移收益。

## 9 月 8 日演进测试提供的证据

这些轮次涉及不同版本、文件大小和对照变量，不与上表混算。

| 对照条件 | 实测结果 | 可支持的结论 |
|---|---|---|
| 每请求 10000 行，packed 均关闭，关闭前移 vs 文件转发，48 并发 | 266.96 → 263.97 秒；目标 BE 14.86 → 7.00 核 | CPU 降低明显，吞吐仅约 +1.1% |
| 同一文件转发路径，48 → 96 并发 | 28.41 → 28.30 请求/秒；平均延迟 1680.10 → 3373.16 ms | 增加并发主要增加等待 |
| 每请求 1000 行，保持文件转发，packed OFF → ON | 93.03 → 456.99 秒 | 该旧版小文件路径启用 packed 后显著变慢，不能泛化为所有 packed 场景 |
| 每请求 1000 行、packed 开启，保持前移，目标 BE 上传 vs Sink 上传；各两轮 | 目标上传 462.12 / 455.83 秒；Sink 上传 49.09 / 47.83 秒 | Sink 上传显著缓解该文件转发场景的等待；这里的 OFF 是关闭 Sink 上传，不是关闭前移 |

Sink 上传另有 120000 次 × 1000 行长测：971.97 秒，123.46 请求/秒；CPU 为 `1.40/1.42/3.88` 核。
它比约 49 秒短测的 152～157 请求/秒低，说明短测峰值不能直接作为持续吞吐；长测没有同规模 OFF 对照。
早期关闭前移两轮耗时为 205.25、269.37 秒，也表明单轮结果会波动，不能只选最快一轮作基线。

## 正确性与适用范围

已离线复核上述轮次的 `requests.jsonl`：正式请求均为 Success、无过滤、单次加载行数符合输入，且请求均分到三个 BE。
普通 DUP 和 Group Commit 最终均为 75300000 行（含预热）；AGG/MOR/MOW 的校验文件与各自聚合或去重口径一致。
MOW 在 compaction 前后的重复 key 查询结果均为空，查询禁用了 `ELIMINATE_GROUP_BY`，避免优化器消除分组后掩盖问题。
约 49 秒的短测最终为 7530000 行，长测为 120030000 行，关闭缓存后的校验也一致，未发现少导入导致“变快”的证据。

9 月 9 日每个模型仅一组 OFF/ON，后台 compaction、缓存和执行顺序仍可能影响结果。
以上属于历史版本、重复样本、单 tablet 的初步实测，不代表多 tablet、生产混合负载或当前 HEAD 的稳定收益。

## 数据来源

机器：`root@172.20.49.6`，根目录：`/root/load_test/`。

- 9 月 8 日：`memtable_ab_20260908/` 下 `packed_off_mem_*`、`packed_switch_*`、`direct_*` 及 `off_1/off_2`。
- 9 月 9 日：`dup_warmup_20260909/`、`agg_warmup_20260909/`、`mor_warmup_20260909/`、`mow_warmup_20260909/` 的 `off_1/on_1`。
- Group Commit：`dup_group_async_20260909/gc_async/`。
- 指标：各轮 `summary.json`、`requests.jsonl`；条件：父目录 `protocol.json`、部署快照及 `run.py`；正确性：`validation*.tsv`、`duplicate_keys*.tsv`。
